import PropTypes from "prop-types";

export const Title = ({ title }) => {
  return (
    <div className=" py-1 text-md font-semibold text-[#1C1C1C] text-opacity-40">
      {title}
    </div>
  );
};
